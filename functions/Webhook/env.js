/** InfluxDB v2 URL */
const url = '';
/** InfluxDB authorization token */
const token = '';
/** Organization within InfluxDB  */
const org = '';
/** InfluxDB bucket used in examples  */
const bucket = '';
// ONLY onboarding example
/** InfluxDB user - Optional one  */
const username = 'my-user';
/** InfluxDB password - Optional one  */
const password = 'test';

const timeout = 500000;

module.exports = {
  url,
  token,
  org,
  bucket,
  username,
  password,
  timeout,
};
